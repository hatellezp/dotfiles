#!/usr/bin/bash
#
#
git config --global alias.ad 'add .'
git config --global alias.can 'commit --amend --no-edit'
git config --global alias.ci 'commit'
git config --global alias.bfp 'push --force-with-lease'
git config --global alias.t 'status'

